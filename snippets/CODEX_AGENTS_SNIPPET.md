# Optional Codex AGENTS.md snippet

Use the `context-optimization` skill when a task involves sparse repositories, uncertain tool availability, context-budget discipline, or a need to prefer targeted reading over broad exploration.

Do not load broad repository context by default. Prefer selective retrieval, then summary, and only treat compression as a late-stage fallback.
